import type { SxProps } from "@mui/system";

export const eventsLayoutStyles: SxProps = {
  display: "flex",
  flexDirection: "column",
  height: "100vh",
  width: "90%",
  overflow: "hidden",
};

export const eventListStyles: SxProps = {
  marginBottom: "1rem",
  overflowY: "auto",
  scrollbarGutter: "stable",
};
