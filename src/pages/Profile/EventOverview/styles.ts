import { pieArcLabelClasses } from "@mui/x-charts";

export const pieChartStyles = {
  [`& .${pieArcLabelClasses.root}`]: {
    fill: "#292929ff",
    fontWeight: "bold",
    fontSize: "0.875rem",
  },
};
