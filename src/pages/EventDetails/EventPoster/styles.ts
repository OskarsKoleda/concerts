import type { SxProps } from "@mui/material";

// TODO: colors not from theme
export const eventPosterStyles: SxProps = {
  boxShadow: "0 20px 40px rgba(0,0,0,0.5)",
  border: "1px solid rgba(255,255,255,0.1)",
};
